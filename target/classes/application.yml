spring:
  application:
    name: task-tracker

  datasource:
    url: jdbc:postgresql://localhost:5433/tasktracker
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET:dGhpc2lzYXZlcnlsb25nc2VjcmV0a2V5Zm9yand0dG9rZW5nZW5lcmF0aW9uYW5kdmFsaWRhdGlvbjEyMzQ1Njc4OQ==}
  expiration: 86400000

cors:
  allowed-origins:
    - http://localhost:5173
    - https://tasker-frontend-xi.vercel.app
